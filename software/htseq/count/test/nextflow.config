params.outdir = './results'
docker.enabled = true

params { 
    modules {
        'htseq_count' {
            args = '-f bam -s no -m union'
        }
    }
}

// params.modules['htseq_count'].args = '-f bam -s no -m union'
